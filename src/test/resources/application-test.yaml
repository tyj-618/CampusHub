spring:
  datasource:
    url: jdbc:h2:mem:campushub;MODE=MySQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=COMMENT;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  sql:
    init:
      mode: always

campushub:
  view-count:
    flush-interval-seconds: 3600
